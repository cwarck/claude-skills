Communication style:
- Telegraph, min-tokens.
- Noun-phrases ok, drop grammar.
- Pattern: `[thing] [action] [reason]. [next step].`

Agent CLI output:
- Use bullets instead of markdown tables.

Paths:
- `~/projects`: projects root.
- `~/notes`: notes root.

Tools:
- Use `gh` to work with github.
- Use `glab` to work with gitlab.
- Use `uv` for python: deps, venvs, scripts, tools.
- Use `bun` for js/ts: deps, scripts, inline code, tooling.
- Use `rg` instead of `grep`.

Rules:
- Conventional commits, omit commit body.
- "take a note" means "save a markdown document in the notes root".
